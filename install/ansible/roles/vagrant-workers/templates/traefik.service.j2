[Unit]
Description=Traefik

[Service]
Type=notify
ExecStart={{ install_dir }}/traefik/bin/traefik --consul --consul.endpoint=127.0.0.1:8500
Restart=always
WatchdogSec=1s

[Install]
WantedBy=multi-user.target